/**
 * About page — temporarily redirected to home.
 * Original content is in page.tsx.bak — restore when artwork is ready.
 */
import { redirect } from "next/navigation";

export default function AboutPage() {
  redirect("/");
}
